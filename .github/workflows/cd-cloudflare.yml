# Deploys to Cloudflare Pages when a PR is merged into main or develop,
# then posts the wrangler deployment summary back to the PR.
#
# REQUIRED REPO SECRETS (Settings -> Secrets and variables -> Actions):
#   CLOUDFLARE_API_TOKEN   token with the "Cloudflare Pages: Edit" permission
#   CLOUDFLARE_ACCOUNT_ID  your Cloudflare account ID
# Both are read automatically by wrangler from the environment.

name: 🚀 Deploy · Cloudflare

on:
  pull_request:
    types: [closed]
    branches: [main, develop]
    # Skip the deploy when a merged PR touches ONLY files that cannot change the
    # built site (CI, docs, editor config, tests, helper scripts). If any file
    # outside this list changed, the deploy still runs. Note: `*.md` matches only
    # root-level docs (README, CLAUDE, ...); blog posts live in content/*.md and
    # are intentionally NOT ignored, so editing a post still deploys.
    paths-ignore:
      - ".github/**"
      - ".claude/**"
      - ".agents/**"
      - ".zed/**"
      - ".storybook/**"
      - "docs/**"
      - "tasks/**"
      - "tests/**"
      - "scripts/**"
      - "*.md"
      - ".gitignore"
      - ".prettierignore"
      - "renovate.json"
      - "skills-lock.json"
      - "tsconfig.scripts.json"
      - "tsconfig.stories.json"
      - "tsconfig.test.json"

permissions:
  contents: read
  pull-requests: write

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - id: deploy
        env:
          # The branch the PR merged into (main or develop). Passing --branch
          # makes Cloudflare route the deploy deterministically — main to the
          # production environment, develop to a named preview — and tags the
          # deployment with correct branch metadata. Without it wrangler guesses
          # from git, which is unreliable in the detached HEAD of a PR checkout
          # and leaves the cleanup workflows unable to identify the branch.
          BRANCH: ${{ github.event.pull_request.base.ref }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -o pipefail
          pnpm build
          pnpm exec wrangler pages deploy --branch "$BRANCH" 2>&1 | tee deploy.log

      - name: Extract wrangler summary
        if: always()
        run: node --experimental-strip-types scripts/github/wrangler-summary.ts deploy.log > wrangler-summary.txt || true

      - name: Bundle report (current)
        if: always()
        run: pnpm exec jiti scripts/github/bundle-report.ts --json > /tmp/current.json || true

      - name: Save bundle baseline
        if: steps.deploy.outcome == 'success'
        uses: actions/cache/save@v4
        with:
          path: /tmp/current.json
          key: bundle-baseline-${{ github.event.pull_request.base.ref }}-${{ github.sha }}

      - name: Upsert PR comment
        if: always()
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require("node:fs");
            const marker = "<!-- cd-cloudflare -->";
            const success = "${{ steps.deploy.outcome }}" === "success";
            const runUrl =
              `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}` +
              `/actions/runs/${context.runId}`;

            let summary = "";
            try {
              summary = fs.readFileSync("wrangler-summary.txt", "utf8").trim();
            } catch {}
            let diff = "";
            try {
              diff = fs.readFileSync("/tmp/diff.md", "utf8").trim();
            } catch {}

            const body = success
              ? `${marker}\n\`\`\`\n${summary || "Deployed."}\n\`\`\`` +
                (diff ? `\n\n${diff}` : "")
              : `${marker}\n❌ Cloudflare Pages deploy failed. ` +
                `See the [workflow run](${runUrl}).`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
            });
            const existing = comments.find((c) => c.body?.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body,
              });
            }
