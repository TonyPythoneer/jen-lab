02 — Caldera-ize shared content components

Status: DONE

- [x] Portal.vue — bg-ash-white / rounded-card / digital-orange/10 chip + text-digital-orange / border + shadow-sm / hover lift
- [x] Product.vue — bg-ash-white / rounded-card / bg-basalt-canvas banner / font-display title / text-abyssal-ink variants / rounded-button CTA
- [x] ImageCarousel.vue — rounded-card
- [x] YoutubeCarousel.vue — rounded-card
- [x] grep: zero teal / #f7f7f7 / rounded-4xl / brutalist shadow in all four files
- [x] typecheck: zero errors in home/ components (pre-existing Header.vue errors unrelated)

Next: 03 — Build shared <ProfilePage> component
