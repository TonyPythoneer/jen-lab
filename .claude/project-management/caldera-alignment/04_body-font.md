# 04 — body-font

## What

`app/assets/css/main.css` — update `--font-sans` to `"DM Sans", "Inter", ui-sans-serif, system-ui, sans-serif`.
Ensure DM Sans is loaded via `@nuxt/fonts` (check `nuxt.config.ts`).

## Verify

Body text renders in DM Sans (check DevTools → Computed → font-family).
