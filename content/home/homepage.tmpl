# 首頁分頁速查

歡迎更新自己的分頁。本檔提供速查,需要更詳細的解說請看 `homepage.complete.tmpl`。

## 檔案放哪

- 分頁內容: `content/home/<分頁名字>.md` (一個檔案 = 一個分頁)
- 圖片資產: `public/home/<分頁名字>/...`
- 在 md 裡引用圖片時,路徑省略 `public`,寫成 `/home/<分頁名字>/...`

## 大致結構

```yaml
---
profile:                # 頭像、名字、簡介
  avatar: ...
  name: ...
  tabs: [...]

sections:               # 抽屜清單,由上而下 = 網頁顯示順序
  - id: ...
    label: ...
    component: ...      # 從下方 4 種挑一個
    <對應欄位>: ...
---
```

## 抽屜種類 (component) — 目前提供 4 種

| component         | 額外欄位         | 用途         |
|-------------------|----------------|------------|
| `portal-list`     | `portals[]`    | 連結卡片清單 |
| `youtube-carousel`| `carousels[]`  | 影片輪播     |
| `image-carousel`  | `carousels[]`  | 圖片輪播     |
| `product-list`    | `products[]`   | 商品卡片     |

未來想要新的抽屜種類 → 跟工程師討論加上去。

- 同一個抽屜內想放多組輪播 → 一個 section 內多寫幾組 `carousels`
- 想拆成獨立抽屜 → 多寫幾個 section

## 最快上手方式

直接複製現有的 `jen-liu.md` 或 `jen-knows.md` 改成自己的內容。範本保留正確的格式與縮排,照著替換文字最不容易出錯。

如果想了解每個欄位的細節,翻 `homepage.complete.tmpl`。

## 小提醒

寫 YAML 時有幾個格式細節要留意,改完若網頁顯示異常,通常是其中一個沒對齊:

- 縮排請用空格 (建議 2 格),避免使用 tab
- `-` 後面要有一個空格
- 多行文字前一行先寫 `|`,下方內容統一往內縮
- 同一檔案內每個 `id` 不要重複
