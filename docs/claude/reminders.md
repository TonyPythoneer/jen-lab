# Claude Reminders

## Language

Always respond in 繁體中文台灣用語. Read the configured language from `~/.claude/settings.json` (`language` field) and follow it for every response, regardless of what language the user writes in.
